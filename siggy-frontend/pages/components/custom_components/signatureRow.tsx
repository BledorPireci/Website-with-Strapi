import React from 'react';
import SignatureCard from "./signatureCard";
import {SignatureTypes} from "../../../lib/types";
import {BadSignature, BadSignature1, BadSignature2, BadSignature3} from "../../assets/assets";


// interface Props{
//     signature:SignatureTypes[]
// }

const SignatureRow = (props: any) => {
    return (
        <div></div>
        // <div className="signatureRow">
        //     <div className="signatureRowContainer">
        //         {signature?.map((sig, index) =>{
        //             return (
        //                 <SignatureCard key={index} signature={sig}/>
        //             )
        //         }
        //
        //         )}
        //     </div>
        // </div>
    );
}
export default SignatureRow;

